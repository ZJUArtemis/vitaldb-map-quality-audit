# Core scientific stack
numpy>=1.23
pandas>=1.5
scipy>=1.9
scikit-learn>=1.2
pyarrow>=10.0          # parquet I/O

# Modelling / interpretability
xgboost>=1.7
shap>=0.41

# Signal processing
neurokit2>=0.2
ruptures>=1.1          # change-point detection

# VitalDB data access
vitaldb>=1.4

# Plotting and utilities
matplotlib>=3.6
tqdm>=4.64

# torch is imported by some scripts only for GPU/seed utilities; CPU build is sufficient
torch>=2.0
